{
  "name": "midsummer-motors",
  "version": "1.0.0",
  "private": true,
  "description": "A first-person Finnish countryside car-building survival game that runs in the browser.",
  "type": "module",
  "scripts": {
    "build": "node tools/build.mjs",
    "dev": "node tools/build.mjs && npx http-server dist -p 8080 -c-1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "three": "0.169.0"
  }
}
